services:
  bandeco:
    image: ghcr.io/jonascardoso/bandeco-unicamp:latest
    container_name: bandeco-unicamp-main
    init: true
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - runtime-cache:/bandeco/.cache_bandeco_nutricao

volumes:
  runtime-cache:
